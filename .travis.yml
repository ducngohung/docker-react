sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t ducngohung/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ducngohung/docker-react npm run test
